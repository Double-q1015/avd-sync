# Dependabot 配置
# 用于自动检查依赖项更新

version: 2
updates:
  # 启用 Python 依赖项更新
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "python"

